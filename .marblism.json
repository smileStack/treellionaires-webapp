{
  "type": "nextjs",
  "version": "1.0.2",
  "commands": {
    "build": "npm run build",
    "start": "npm run database:sync && npm run start"
  },
  "commandsWorkspace": {
    "init": "npm install && npm run global:sync ; npm run database:seed",
    "dev": "npm run dev",
    "syncModels": "npm run global:sync"
  }
}
